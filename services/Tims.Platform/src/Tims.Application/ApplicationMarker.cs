namespace Tims.Application;

/// <summary>
/// Assembly marker for Tims.Application. Use cases land here as the platform grows
/// (Phase 2+). Present now so the project is non-empty and ArchitectureTests can assert
/// its reference set (Domain only).
/// </summary>
public static class ApplicationMarker;
