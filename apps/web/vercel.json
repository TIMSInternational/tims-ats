{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "pnpm --filter @tims/db exec prisma generate --schema prisma/schema && next build",
  "regions": ["pdx1"],
  "crons": [
    {
      "path": "/api/cron/evaluate-alerts",
      "schedule": "0 6 * * *"
    }
  ]
}
