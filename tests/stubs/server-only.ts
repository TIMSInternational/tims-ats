// Test stub for the `server-only` marker package. The real module's default
// export throws unconditionally outside the Next.js server bundler, which would
// break vitest imports of server-only helpers. Aliased in vitest.config.ts.
export {};
